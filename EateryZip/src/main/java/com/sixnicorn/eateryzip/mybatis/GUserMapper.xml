<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Guser">

	<!-- select의 결과가 없으면 0이 된다.-->
	<select id="getData" parameterType="string" resultType="GuserDto">
	select g_id, g_pwd, g_name, g_address, g_email, g_phone, g_profile, g_regdate, grade
	from g_user
	where g_id = #{g_id}	
	</select>
	
	<insert id="insert" parameterType="GuserDto">
	insert into g_user
	(g_id, g_pwd, g_name, g_address, g_email, g_phone, g_regdate, grade)
	values(#{g_id}, #{g_pwd}, #{g_name}, #{g_address},  #{g_email}, #{g_phone}, sysdate, #{grade})
	</insert>
	
	<select id="isExist" parameterType="java.lang.String" resultType="string">
	Select g_id
	from g_user
	where g_id=#{g_id} 
	</select>

</mapper>